class triangleArea:

    base = input('Place input the base of the triangle: ')
    height = input('Please input the height of the triangle: ')
    area = 0.5 * base * hieght 

    def __init__(self):
    
        self.base = base
        self.height = height
        self.area = area 
        
    def areaOfTriangle(self):
    
        if self.base == True and self.hieght == True:
            print('The area of your triangle is: ' self.area)
        else:
            print('Please input the dimensions first.')
            triangleArea():
            
triangleArea():

            
